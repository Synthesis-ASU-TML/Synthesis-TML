// Eerm Test Stuff.#ifndef _eermTestI_h#define _eermTestI_h#include eermI.h// The structure validity tests // are contained in the header eerm.h// They contain assertions rather than // returning a bool.// Need to find a better way to encapsulate // this later:/*TBool VectorIsPositive(float *w, int n);TBool WeightIsValid(float *w, int n);TBool VectorIsTangent(float *w, int n);TBool NodeIsValid(Node *n);TBool NodeListIsValid(Node **n, int Nn);TBool TokenIsValid(Token *t);TBool SimplexIsValid(Simplex *s);TBool FeatureIsValid(Feature *f);TBool EermIsValid(Eerm *e);*/#endif